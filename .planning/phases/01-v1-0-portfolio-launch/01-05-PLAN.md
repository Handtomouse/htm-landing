---
phase: 01-v1-0-portfolio-launch
plan: 05
type: execute
wave: 3
depends_on: [01-04]
files_modified:
  - app/work/components/Testimonial.tsx
  - app/work/components/DeliverablesIndex.tsx
  - app/work/portfolio.css
autonomous: true
requirements: [POLISH-01, POLISH-02, DATA-03]
must_haves:
  truths:
    - "Testimonial component renders the quote + author for the 6 cases that have an attributed quote (swich, fishbowl, sbts, maplemoon, ferrari, troodpratt)"
    - "Testimonial component renders null for the 13 cases without an attributed quote (no empty box, no TODO placeholder) — DATA-03 graceful-degrade"
    - "Testimonial component scale: 28-32px quote, line-height 1.3, max-width 28ch, 120px serif quote-glyph at 15% opacity behind first word, 1px amber rule above attribution (POLISH-01)"
    - "DeliverablesIndex rows reveal a 96x96 hover thumbnail (heroImg as fallback) on pointer-fine devices (POLISH-02)"
    - "DeliverablesIndex rows are anchor links to the case imagery section (#imagery) with a trailing ↗ glyph"
  artifacts:
    - path: "app/work/components/Testimonial.tsx"
      provides: "Polished testimonial render (POLISH-01) with graceful absence (DATA-03)"
      min_lines: 25
    - path: "app/work/components/DeliverablesIndex.tsx"
      provides: "Polished deliverables list with hover thumb + anchor link (POLISH-02)"
      min_lines: 30
  key_links:
    - from: "app/work/components/Testimonial.tsx"
      to: "testimonial.q + testimonial.a"
      via: "conditional render"
      pattern: "testimonial\\.a"
    - from: "app/work/components/DeliverablesIndex.tsx"
      to: "heroImg as hover-thumb fallback"
      via: "img src binding"
      pattern: "heroImg"
---

<objective>
Overwrite the Plan 04 stubs for Testimonial + DeliverablesIndex with polished implementations that satisfy POLISH-01 (testimonial scale + serif glyph), POLISH-02 (deliverables hover thumb + click-to-section), and DATA-03 (testimonial renders gracefully for cases with quotes, and renders nothing for cases without).

Purpose: Plan 04 ships static stubs so the build doesn't break while these two components are developed in parallel. This plan delivers the polished UX.

Per locked default #9: testimonial component must render correctly for the 6 cases that have an attributed quote (swich, fishbowl, sbts, maplemoon, ferrari, troodpratt — confirmed in RESEARCH line 694). For the other 13 cases the component returns null. No empty boxes, no "TODO" placeholders, no missing-quote scaffolding.

Output: Two component files completely overwritten. Build passes. Visual screenshot of swich (testimonial present) shows the serif quote glyph + amber rule. Visual screenshot of pureairo (no testimonial — research line 696 confirms "No client quote on file yet") shows the section absent entirely.

Carry-forward constraints (apply throughout):
- No em-dashes in user-facing strings (CSS/JS comments OK)
- No fake/placeholder content (DATA-03 — missing quotes do NOT get fabricated)
- Sign-off / commit author: "Nate"
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-v1-0-portfolio-launch/01-RESEARCH.md
@.planning/phases/01-v1-0-portfolio-launch/01-02-SUMMARY.md
@.planning/phases/01-v1-0-portfolio-launch/01-03-SUMMARY.md

<interfaces>
<!-- Types defined in Plan 04 Task 1 (app/work/components/types.ts): -->
<!-- export type TestimonialProps = { testimonial?: Testimonial }; -->
<!-- export type DeliverablesIndexProps = { -->
<!--   deliverables: string[]; -->
<!--   heroImg: string; -->
<!--   caseSlug: string; -->
<!-- }; -->

<!-- Testimonial type from lib/cases.ts: -->
<!-- export type Testimonial = { q: string; a: string }; -->
<!-- Empty a = '' means no attributed quote (13 cases). Render null in that case. -->

<!-- 6 cases known to have real attributed quotes (per RESEARCH line 694): -->
<!--   swich, fishbowl, sbts, maplemoon, ferrari, troodpratt -->
<!-- 13 cases without: -->
<!--   mdb, bankwest, refundid, pureairo, allianz, endoca, hills, stealth, softlite, mullet, aura, salesforce, verde -->

<!-- POLISH-01 spec from HANDOFF.md (lines 26-36): -->
<!--   - Scale quote to 28-32px, line-height 1.3, max-width 28ch -->
<!--   - 120px serif " glyph behind the first word at 15% opacity -->
<!--   - 1px amber rule above attribution -->
<!--   - Watermark data-num lives at top:50% right:4% — quote glyph at top-left to avoid collision -->

<!-- POLISH-02 spec from HANDOFF.md (lines 38-51): -->
<!--   - Hover on each row slides in 96x96 thumbnail from the right -->
<!--   - Click jumps to case imagery section (#imagery anchor used by CaseDetail) -->
<!--   - Trailing ↗ glyph signals interactivity -->
<!--   - heroImg as the hover-thumb fallback (no per-row mapping in scope for v1) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement polished Testimonial component (POLISH-01 + DATA-03)</name>
  <files>app/work/components/Testimonial.tsx, app/work/portfolio.css</files>
  <read_first>
    - app/work/components/types.ts (TestimonialProps shape — Plan 04)
    - app/work/components/Testimonial.tsx (Plan 04 stub — confirm it currently returns null)
    - /Users/handtomouse/UFC/spins/htm_portfolio_v3_polish_round2_20260520/HANDOFF.md (lines 22-36, POLISH-01 full spec)
    - /Users/handtomouse/UFC/spins/htm_v2_5round_loop_20260517/mockups/04_portfolio_v2.html (the canonical `.testimonial` CSS, roughly line 604 post-batch-A; and the `data-num` watermark rule near line 315)
    - lib/cases.json (confirm the 6 cases with non-empty testimonial.a values)
    - app/work/portfolio.css (current `.testimonial` styles inherited from canonical — to know what to override vs preserve)
  </read_first>
  <action>
    **(A) Overwrite `app/work/components/Testimonial.tsx`** with the polished implementation:

    ```tsx
    // app/work/components/Testimonial.tsx
    // POLISH-01: testimonial scale + serif glyph + amber rule.
    // DATA-03: render null if testimonial absent OR author empty (no fabricated content).
    // The 6 cases with real quotes (per research): swich, fishbowl, sbts, maplemoon, ferrari, troodpratt.
    // The 13 without get a clean absence — no empty box, no TODO placeholder.
    import type { TestimonialProps } from './types';

    export default function Testimonial({ testimonial }: TestimonialProps) {
      // DATA-03 graceful absence: render nothing for cases without an attributed quote.
      if (!testimonial || !testimonial.q || !testimonial.a || testimonial.a.trim() === '') {
        return null;
      }

      const { q, a } = testimonial;

      return (
        <figure className="testimonial testimonial-polished" data-has-quote="true">
          {/* POLISH-01: 120px serif quote-glyph at 15% opacity, top-left to avoid data-num at right:4% */}
          <span className="testimonial-glyph" aria-hidden="true">{'“'}</span>
          <blockquote className="testimonial-quote">
            {/* Scale: 28-32px, line-height 1.3, max-width 28ch (set via portfolio.css below) */}
            {q}
          </blockquote>
          {/* POLISH-01: 1px amber rule above attribution */}
          <hr className="testimonial-rule" aria-hidden="true" />
          <figcaption className="testimonial-author">{a}</figcaption>
        </figure>
      );
    }
    ```

    **(B) Append POLISH-01 styles to `app/work/portfolio.css`** (do not remove canonical's existing `.testimonial` rule; add a more-specific override under `.testimonial-polished`):

    ```css
    /* POLISH-01: testimonial scale + serif glyph + amber rule
       Overrides canonical .testimonial defaults with more-specific class. */
    .htm-portfolio .testimonial-polished {
      position: relative;
      max-width: 28ch;
      margin: 4rem auto;
      padding: 2rem 1.5rem 1.5rem;
    }
    .htm-portfolio .testimonial-polished .testimonial-glyph {
      position: absolute;
      top: 0;
      left: 0;
      font-family: Georgia, 'Times New Roman', serif;
      font-size: 120px;
      line-height: 1;
      color: var(--amber, #f7a835);
      opacity: 0.15;
      pointer-events: none;
      user-select: none;
    }
    .htm-portfolio .testimonial-polished .testimonial-quote {
      font-size: clamp(28px, 3vw, 32px);
      line-height: 1.3;
      font-style: italic;
      margin: 0 0 1rem 0;
      position: relative;
      z-index: 1;
    }
    .htm-portfolio .testimonial-polished .testimonial-rule {
      border: 0;
      border-top: 1px solid var(--amber, #f7a835);
      margin: 1rem 0 0.5rem 0;
      width: 4rem;
    }
    .htm-portfolio .testimonial-polished .testimonial-author {
      font-size: 0.875rem;
      letter-spacing: 0.05em;
      text-transform: uppercase;
      opacity: 0.8;
    }
    ```

    **(C) Verify DATA-03 graceful absence.** Hand-check (executor confirms): build the dev server, navigate to `/work/pureairo` (a case with empty author), screenshot, and confirm no testimonial section appears in the DOM.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -20 &amp;&amp; grep -c "testimonial.a.trim() === ''" app/work/components/Testimonial.tsx &amp;&amp; grep -c "testimonial-glyph" app/work/components/Testimonial.tsx &amp;&amp; grep -c "testimonial-polished" app/work/portfolio.css &amp;&amp; (npm run dev &amp; SERVER_PID=$!; sleep 5; bash scripts/smoke-screenshot.sh http://localhost:3000/work/swich /tmp/case-swich-testimonial.png 1280 1600 &amp;&amp; bash scripts/smoke-screenshot.sh http://localhost:3000/work/pureairo /tmp/case-pureairo-no-testimonial.png 1280 1600 &amp;&amp; curl -s http://localhost:3000/work/pureairo | grep -c "testimonial-polished"; kill $SERVER_PID 2>/dev/null; test -f /tmp/case-swich-testimonial.png &amp;&amp; test -f /tmp/case-pureairo-no-testimonial.png)</automated>
  </verify>
  <acceptance_criteria>
    - `app/work/components/Testimonial.tsx` exists and is no longer a stub (>= 25 lines including comments)
    - `grep -c "testimonial.a.trim() === ''" app/work/components/Testimonial.tsx` returns >= 1 (graceful absence guard)
    - `grep -c "testimonial-glyph" app/work/components/Testimonial.tsx` returns >= 1 (POLISH-01 glyph element)
    - `grep -c "testimonial-rule" app/work/components/Testimonial.tsx` returns >= 1 (POLISH-01 amber rule)
    - `grep -c "testimonial-polished" app/work/portfolio.css` returns >= 4 (CSS scope, glyph, quote, rule, author all addressed)
    - `npm run build` exits 0
    - Live check: `curl -s http://localhost:3000/work/swich | grep -c "testimonial-polished"` returns >= 1 (renders for cases with quotes)
    - Live check: `curl -s http://localhost:3000/work/pureairo | grep -c "testimonial-polished"` returns 0 (DATA-03 absence verified)
    - All 6 quote-bearing cases (swich, fishbowl, sbts, maplemoon, ferrari, troodpratt) render the testimonial: confirm with a loop `for slug in swich fishbowl sbts maplemoon ferrari troodpratt; do curl -s http://localhost:3000/work/$slug | grep -q "testimonial-polished" || echo "MISSING ON $slug"; done` produces no output
    - All 13 non-quote cases render no testimonial: confirm with a loop `for slug in mdb bankwest refundid pureairo allianz endoca hills stealth softlite mullet aura salesforce verde; do curl -s http://localhost:3000/work/$slug | grep -q "testimonial-polished" && echo "UNEXPECTED ON $slug"; done` produces no output
    - Visual screenshot `/tmp/case-swich-testimonial.png` shows the serif quote glyph + amber rule (manual check)
    - Visual screenshot `/tmp/case-pureairo-no-testimonial.png` shows no testimonial section (manual check)
  </acceptance_criteria>
  <done>Testimonial renders polished UI for 6 quote-bearing cases; renders null for 13 others; build passes; live + visual verification confirmed at quote-present + quote-absent boundaries.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement polished DeliverablesIndex component (POLISH-02)</name>
  <files>app/work/components/DeliverablesIndex.tsx, app/work/portfolio.css</files>
  <read_first>
    - app/work/components/types.ts (DeliverablesIndexProps shape — Plan 04)
    - app/work/components/DeliverablesIndex.tsx (Plan 04 stub — confirm current naive impl)
    - /Users/handtomouse/UFC/spins/htm_portfolio_v3_polish_round2_20260520/HANDOFF.md (lines 38-51, POLISH-02 spec)
    - app/work/components/CaseDetail.tsx (Plan 04 — confirm `#imagery` anchor exists on the case-imagery section so the deliverable click-link target is real)
    - app/work/portfolio.css (existing canonical `.ddeliverables` styles, if any)
  </read_first>
  <action>
    Per POLISH-02 spec: each deliverable row reveals a 96x96 hover thumbnail (heroImg as fallback per HANDOFF.md "uses the case's heroImg or first image as fallback"), clicking jumps to the case imagery section via `#imagery` anchor, trailing `↗` glyph signals interactivity.

    Per HANDOFF.md note: "For round 2, just implement HOVER THUMB (uses the case's heroImg or first image as fallback) + click-to-imagery-section (not specific artboard). Specific-artboard mapping is a content task best done case-by-case." This is the round 2 scope; specific-artboard mapping deferred.

    **(A) Overwrite `app/work/components/DeliverablesIndex.tsx`**:

    ```tsx
    // app/work/components/DeliverablesIndex.tsx
    // POLISH-02: deliverables index with hover thumb + click-to-imagery-section.
    // Round 2 scope per HANDOFF.md: heroImg as universal hover-thumb fallback.
    // Specific-artboard mapping is deferred (content task, per-case).
    // Server Component: CSS handles hover behavior (no client JS needed for pointer-fine reveal).
    import type { DeliverablesIndexProps } from './types';

    export default function DeliverablesIndex({ deliverables, heroImg, caseSlug }: DeliverablesIndexProps) {
      if (!Array.isArray(deliverables) || deliverables.length === 0) return null;

      return (
        <section className="deliverables-section" aria-labelledby={`deliverables-${caseSlug}`}>
          <h2 id={`deliverables-${caseSlug}`} className="deliverables-heading">Deliverables</h2>
          <ol className="ddeliverables ddeliverables-polished">
            {deliverables.map((d, i) => (
              <li key={`${caseSlug}-${i}`} className="deliverable-row">
                <a href="#imagery" className="deliverable-link">
                  <span className="deliverable-num">{String(i + 1).padStart(2, '0')}</span>
                  <span className="deliverable-name">{d}</span>
                  {/* POLISH-02: trailing ↗ glyph signals interactivity */}
                  <span className="deliverable-arrow" aria-hidden="true">{'↗'}</span>
                  {/* POLISH-02: 96x96 hover thumb, heroImg as fallback */}
                  <img
                    src={heroImg}
                    alt=""
                    aria-hidden="true"
                    className="deliverable-thumb"
                    loading="lazy"
                  />
                </a>
              </li>
            ))}
          </ol>
        </section>
      );
    }
    ```

    **(B) Append POLISH-02 styles to `app/work/portfolio.css`** (do not remove canonical `.ddeliverables` rule; layer the polished class):

    ```css
    /* POLISH-02: deliverables hover thumb + click-to-section */
    .htm-portfolio .ddeliverables-polished {
      list-style: none;
      padding: 0;
      margin: 2rem 0;
    }
    .htm-portfolio .deliverable-row {
      border-top: 1px solid rgba(238, 234, 226, 0.12);
    }
    .htm-portfolio .deliverable-row:last-child {
      border-bottom: 1px solid rgba(238, 234, 226, 0.12);
    }
    .htm-portfolio .deliverable-link {
      display: flex;
      align-items: center;
      gap: 1rem;
      padding: 1rem 0;
      color: inherit;
      text-decoration: none;
      position: relative;
      transition: background 0.15s ease;
    }
    .htm-portfolio .deliverable-link:hover,
    .htm-portfolio .deliverable-link:focus {
      background: rgba(247, 168, 53, 0.04);
    }
    .htm-portfolio .deliverable-num {
      font-family: var(--font-vt323), monospace;
      font-size: 1.25rem;
      opacity: 0.6;
      min-width: 2.5rem;
    }
    .htm-portfolio .deliverable-name {
      flex: 1;
      font-size: 1rem;
    }
    .htm-portfolio .deliverable-arrow {
      font-size: 1rem;
      opacity: 0;
      transform: translateX(-4px);
      transition: opacity 0.2s ease, transform 0.2s ease;
    }
    .htm-portfolio .deliverable-link:hover .deliverable-arrow,
    .htm-portfolio .deliverable-link:focus .deliverable-arrow {
      opacity: 1;
      transform: translateX(0);
    }
    .htm-portfolio .deliverable-thumb {
      position: absolute;
      right: 0;
      top: 50%;
      width: 96px;
      height: 96px;
      object-fit: cover;
      transform: translateY(-50%) translateX(120%);
      opacity: 0;
      transition: transform 0.25s ease, opacity 0.2s ease;
      pointer-events: none;
      border: 1px solid rgba(247, 168, 53, 0.4);
      z-index: 2;
    }
    /* Pointer-fine only — touch devices skip the hover-thumb */
    @media (hover: hover) and (pointer: fine) {
      .htm-portfolio .deliverable-link:hover .deliverable-thumb,
      .htm-portfolio .deliverable-link:focus .deliverable-thumb {
        transform: translateY(-50%) translateX(0);
        opacity: 1;
      }
    }
    ```
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -20 &amp;&amp; grep -c "deliverable-thumb" app/work/components/DeliverablesIndex.tsx &amp;&amp; grep -c "u2197\|↗" app/work/components/DeliverablesIndex.tsx &amp;&amp; grep -c "href=\"#imagery\"" app/work/components/DeliverablesIndex.tsx &amp;&amp; grep -c "hover: hover" app/work/portfolio.css &amp;&amp; (npm run dev &amp; SERVER_PID=$!; sleep 5; bash scripts/smoke-screenshot.sh http://localhost:3000/work/swich /tmp/case-swich-deliverables.png 1280 2000; kill $SERVER_PID 2>/dev/null; test -f /tmp/case-swich-deliverables.png)</automated>
  </verify>
  <acceptance_criteria>
    - `app/work/components/DeliverablesIndex.tsx` is no longer the Plan 04 stub (>= 30 lines)
    - `grep -c "deliverable-thumb" app/work/components/DeliverablesIndex.tsx` returns >= 1 (hover thumb element present)
    - `grep -E "u2197|↗" app/work/components/DeliverablesIndex.tsx` returns >= 1 (arrow glyph present)
    - `grep -c "href=\"#imagery\"" app/work/components/DeliverablesIndex.tsx` returns >= 1 (anchor target matches CaseDetail's #imagery section)
    - `grep -c "@media (hover: hover)" app/work/portfolio.css` returns >= 1 (pointer-fine guard present)
    - `grep -E "96px" app/work/portfolio.css | grep -c "deliverable-thumb"` returns >= 1 OR `grep -B1 -A2 "deliverable-thumb" app/work/portfolio.css | grep -c "96px"` returns >= 1 (96x96 sizing confirmed in proximity)
    - `npm run build` exits 0
    - Live check: `curl -s http://localhost:3000/work/swich | grep -c "deliverable-link"` returns >= 1 (component renders)
    - Live check: clicking a deliverable link in the screenshot would navigate to `/work/swich#imagery` (verified by anchor existence: `curl -s http://localhost:3000/work/swich | grep -c 'id="imagery"'` returns >= 1)
    - Visual screenshot `/tmp/case-swich-deliverables.png` shows the deliverables list (manual hover check deferred to QA wave)
    - For cases without deliverables array, the section is absent: confirm at least one case where deliverables is empty/missing has no `deliverables-section` in the rendered HTML
  </acceptance_criteria>
  <done>DeliverablesIndex renders with hover thumb + arrow glyph + #imagery anchor; build passes; visual screenshot captured; #imagery target exists in CaseDetail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Case testimonial / deliverables data | Build-time JSON, trusted source (Nate-authored from real clients) |
| Hover-thumb image src | Bound to heroImg, already validated by Plan 02 asset-existence audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering (XSS) | Testimonial quote text | mitigate | React auto-escapes string children; q/a rendered as plain text |
| T-05-02 | Information Disclosure | Anonymous attribution | accept | All 6 attributed quotes have real authors who consented to public display |
| T-05-03 | Information Disclosure | DATA-03 false-positive (placeholder content rendering) | mitigate | Strict `testimonial.a.trim() === ''` guard returns null; verified across all 13 non-quote cases |
</threat_model>

<verification>
- npm run build exits 0
- Testimonial component renders for all 6 quote-bearing slugs (verified via curl loop)
- Testimonial component renders nothing for all 13 non-quote slugs (verified via curl loop)
- POLISH-01 spec elements present: glyph (“), polished class scope, amber rule, scale via clamp(28px, 3vw, 32px)
- POLISH-02 spec elements present: hover thumb, ↗ arrow glyph, #imagery anchor target, pointer-fine media query guard
</verification>

<success_criteria>
- POLISH-01: Testimonial scale + serif glyph + amber rule rendered
- POLISH-02: Deliverables hover thumb + click-to-section + ↗ glyph rendered
- DATA-03: Testimonial absent without empty box on all 13 non-quote cases; present on all 6 quote cases
</success_criteria>

<output>
After completion, create `.planning/phases/01-v1-0-portfolio-launch/01-05-SUMMARY.md` recording: the 6/13 split confirmed by live curl loops, screenshot paths, and any visual deviations from canonical noted during impl.
</output>
